
//export 
const score = () => {
  //Create object?  Including all letters along with their points
  //Scrabble object - keys = the score , value = array of letters?  
  //OR variables as the keys that are arrays with the letters, values with the score

  //Break string into array
  //Convert the object keys into their arrays...?  Create function / loop thru the arrKeys along the scrabbleArr

  //Push matching values/key scores into array to add up later
  //OR use reduce to add all the values that match to return
};
